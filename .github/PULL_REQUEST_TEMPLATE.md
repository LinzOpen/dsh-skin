## 这个 PR 做了什么

<!-- 一两句。为什么需要它，比它改了什么更重要。 -->

## 自检

- [ ] `npm run check` 通过（仓库自检 + 单元测试 + 皮肤校验）
- [ ] 改了程序或 SDK 的话，`npx electron app/test/smoke.js` 和
      `npx electron packages/host/test/smoke.js` 都通过

### 如果这个 PR 带了一套皮肤

- [ ] `skin.json` 写了 `license`
- [ ] 我有权公开里面的每一份素材（官方立绘和他人同人图不算）
- [ ] 附了明暗两种模式的截图
- [ ] 有 `palette.json` 的话跑过 `npm run build:skins`
- [ ] 留下的 `warn` 我在下面说明了原因

<!-- warn 的说明写这里 -->
